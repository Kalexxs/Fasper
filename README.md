# FASPER Unidade 1 Protótipo:

Abra `frontend/index.html` no navegador.

Sem Node e sem fontes por URL.
Os botões Salvar exportam arquivos Excel (`.xls`) e os dados também ficam no `localStorage` para preencher a tela de resumo.
